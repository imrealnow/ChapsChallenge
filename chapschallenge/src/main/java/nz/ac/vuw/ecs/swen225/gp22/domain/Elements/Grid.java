package main.java.nz.ac.vuw.ecs.swen225.gp22.domain.Elements;

/**
 * A Grid represents a 'solid' object that should stop the player from moving on top of it if they try to.
 */
class Grid extends Tile{

    /**
     * Constructs a grid at the specified position.
     * This class is unfinished as grids should not be contructed without a sprite to represent them.
     * 
     * @param x
     * @param y
     */
    public Grid(int x, int y){
        super(x,y);
    }
}